Lucide icon (1.75 stroke) — use for every glyph in the product; never emoji or hand-drawn SVG.

```jsx
<Icon name="car" size={18} />
```

Requires `<script src="https://unpkg.com/lucide@0.460.0/dist/umd/lucide.min.js">`. Common names: car, calendar-days, file-text, credit-card, users, building-2, printer, plus, search, chevron-down, check, x, alert-triangle.
