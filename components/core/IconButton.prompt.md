IconButton — toolbar and table-row actions (edit, print, delete). "dark" variant sits on the navy sidebar/topbar.

```jsx
<IconButton icon="printer" label="Printo" />
<IconButton icon="bell" label="Njoftime" variant="dark" />
```
