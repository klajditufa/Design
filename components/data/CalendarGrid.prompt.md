CalendarGrid — the booking calendar; each event spans from→to day of the month.

```jsx
<CalendarGrid year={2026} month={9} today={11} events={[{id:1,label:'Yaris · A. Hoxha',from:10,to:14,status:'active'}]} />
```
