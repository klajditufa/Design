Input — 38px, 1px border, navy focus ring. Use mono for plates/IDs/amounts, suffix for "ALL"/"€"/"ditë".

```jsx
<Input icon="search" placeholder="Kërko klient, targë…" />
<Input mono suffix="ALL" defaultValue="12.500" />
```
